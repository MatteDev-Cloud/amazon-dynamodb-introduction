# Uso: npm run dev (vite, modalità "development" di default)
# Iterazione locale: punta al backend locale avviato dalla root con `npm run dev:api`
# (richiede DynamoDB Local via `docker compose up -d` + `npm run seed`).
VITE_API_BASE=http://localhost:3001
VITE_SESSION=prova-01
VITE_DOCUMENT_URL=/dynamolive-approfondimento.pdf
VITE_BACKUP_URL=/backup.mp4
