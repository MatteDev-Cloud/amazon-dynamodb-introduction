{
  "name": "dynamolive-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build:dev": "tsc --noEmit && vite build --mode dev",
    "build:live": "tsc --noEmit && vite build --mode live",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --experimental-transform-types --test src/test/*.test.ts"
  },
  "dependencies": {
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "1.5.5",
    "typescript": "5.9.3",
    "vite": "7.3.6"
  }
}
